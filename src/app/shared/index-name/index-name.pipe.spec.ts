import { IndexNamePipe } from './index-name.pipe';

describe('IndexNamePipe', () => {
    it('create an instance', () => {
        const pipe = new IndexNamePipe();
        expect(pipe).toBeTruthy();
    });
});
